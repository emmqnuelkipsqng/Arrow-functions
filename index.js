// Define the divide function
const divide = () => {
    return 2000 / 100;
  };
  
  // Define the square function
  const square = (x) => {
    return x * x;
  };
  
  // Define the add function
  const add = (a, b) => {
    return a + b;
  };